{
  "name": "trasferirsi-italia",
  "type": "module",
  "version": "0.1.0",
  "description": "Authoritative, dated, primary-source-cited reference for relocating to Italy. v1: the EU-citizen national residency journey.",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run check:freshness && npm run check:figures && npm run check:theme && astro build && npm run check:seo && npm run check:journeys && npm run check:prose",
    "preview": "astro preview",
    "check:freshness": "node scripts/check-freshness.mjs",
    "check:figures": "node scripts/check-figures.mjs",
    "check:theme": "node scripts/check-theme.mjs",
    "check:links": "node scripts/check-links.mjs",
    "fetch:archives": "node scripts/fetch-archives.mjs",
    "astro": "astro",
    "submit:indexnow": "node scripts/submit-indexnow.mjs",
    "check:seo": "node scripts/check-seo.mjs",
    "check:journeys": "node scripts/check-journeys.mjs",
    "check:prose": "node scripts/check-prose.mjs",
    "build:history": "node scripts/build-history.mjs",
    "gen:og": "node scripts/gen-og.mjs",
    "test:smoke": "node scripts/smoke.mjs",
    "test:gates": "node scripts/test-gates.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "@fontsource-variable/fraunces": "^5.2.9",
    "@fontsource-variable/inter": "^5.2.8",
    "@vercel/analytics": "^2.0.1",
    "astro": "^5.1.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "axe-core": "^4.10.0",
    "playwright-core": "^1.49.0",
    "tailwindcss": "^4.0.0"
  }
}
